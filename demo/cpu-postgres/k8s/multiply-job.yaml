apiVersion: batch/v1
kind: Job
metadata:
  generateName: he-salary-multiply-
  namespace: ${DEMO_NAMESPACE}
  labels:
    app.kubernetes.io/name: he-salary-multiply
    app.kubernetes.io/part-of: cpu-postgres-demo
    he.demo/stage: multiply
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 900
  template:
    metadata:
      labels:
        app.kubernetes.io/name: he-salary-multiply
        app.kubernetes.io/part-of: cpu-postgres-demo
        he.demo/stage: multiply
    spec:
      nodeSelector:
        kubernetes.io/hostname: ${DEMO_NODE_NAME}
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      restartPolicy: Never
      automountServiceAccountToken: false
      containers:
        - name: secretless-multiply-evaluator
          image: ${DEMO_HE_IMAGE}
          imagePullPolicy: Always
          command:
            - python
            - -m
            - cpu_postgres_demo.cli
          args:
            - multiply
          envFrom:
            - configMapRef:
                name: cpu-postgres-demo-config
            - secretRef:
                name: cpu-postgres-demo-db
          resources:
            requests:
              cpu: "1"
              memory: 2Gi
            limits:
              cpu: "4"
              memory: 6Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: temporary-files
              mountPath: /tmp
      volumes:
        - name: temporary-files
          emptyDir:
            sizeLimit: 512Mi
